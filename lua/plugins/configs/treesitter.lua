local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "c", "c_sharp", "python", "markdown", "markdown_inline" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
